<div class="container-fluid header-title">
  <div class="row">
    <div class="col-lg-4 pt-3"><p class="currency px-2 font-weight-bold">GBP £</p></div>
    <div class="text-center col-lg-4 pb-1 pt-4">
       <%= link_to root_path, class: "brand-title" do %>
         <h1 class="brand-title">MANNEQUINN</h1>
       <% end %>
    </div>
    <div class="user-corner col-lg-4 pt-3 d-flex justify-content-end align-items-start"">
      <% if user_signed_in? %>
        <p class="fs-1 px-2"><i class="fas fa-user"></i> <%= current_user.username.downcase %></p>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, remote: false, class: 'fs-1 px-2' %>
      <% end %>
    </div>
  </div>
</div>
