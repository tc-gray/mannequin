<%= simple_form_for [@user, @product] do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :category, collection: Product::CATEGORYS, include_blank: false%>
    <%= f.input :description %>
    <%= f.input :size, collection: Product::SIZES, include_blank: false %>
    <%= f.input :price %>
    <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  </div>

  <div class="form-actions btn btn-book">
    <%= f.button :submit %>
  </div>
<% end %>
